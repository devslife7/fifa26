# Project Instructions

- When starting local dev or test servers for this repo, use port 7000 or higher.
- If a script defaults to a lower port, override it for the session instead of using that lower port.
- Prefer port 7000 first, then 7001, 7002, and so on if the port is occupied.
